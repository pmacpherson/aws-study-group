var config = {};

// for local development, update path to aws.json 
//config.aws = "./aws.json";
// for production development, do not use aws.json or AWS Access keys!
config.aws = null;

module.exports = config;
